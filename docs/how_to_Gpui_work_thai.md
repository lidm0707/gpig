# GPUI ทำงานอย่างไร: สถาปัตยกรรมและภาพรวม

## สารบัญ
1. [ภาพรวม](#ภาพรวม)
2. [สถาปัตยกรรมระดับสูง](#สถาปัตยกรรมระดับสูง)
3. [ออบเจกต์ App](#ออบเจกต์-app)
4. [ระบบ Entity](#ระบบ-entity)
5. [ประเภท Context](#ประเภท-context)
6. [การจัดการ State](#การจัดการ-state)
7. [ขั้นตอนการ Render](#ขั้นตอนการ-render)
8. [ระบบเหตุการณ์](#ระบบเหตุการณ์)
9. [โมเดลประสิทธิภาพ](#โมเดลประสิทธิภาพ)
10. [การเปรียบเทียบกับ React](#การเปรียบเทียบกับ-react)

---

## ภาพรวม

GPUI (GPU User Interface) เป็นเฟรมเวิร์ก UI ที่พัฒนาด้วยภาษา Rust ที่รวมแนวทางการ render แบบ immediate และ retained mode 不同于传统的 UI เฟรมเวิร์ก, GPUI ใช้สถาปัตยกรรมแบบ entity-based ที่ state ทั้งหมดถูกครอบครองโดยออบเจกต์ App กลาง และ UI components (entities) สังเกตและ render state เหล่านั้น

**ลักษณะสำคัญ:**
- 🎨 GPU-accelerated rendering (คำสั่ง GPU โดยตรง)
- 🧠 Entity-based architecture (state อยู่ใน entities)
- 🔍 Selective observation (เฉพาะ entities ที่สังเกตเท่านั้นที่ re-render)
- ⚡ Zero virtual DOM (render โดยตรง)
- 🔄 Hybrid mode (immediate + retained)

---

## สถาปัตยกรรมระดับสูง

```
┌─────────────────────────────────────────────────────────────┐
│                        Application                          │
│                                                              │
│  ┌───────────────────────────────────────────────────────┐  │
│  │                      App Object                        │  │
│  │  • ครอบครอง entities ทั้งหมด                          │
│  │  • จัดการ lifecycles ของ entities                    │
│  │  • ประสานการ render                                   │
│  │  • จัดการ window management                          │  │
│  └───────────────────────────────────────────────────────┘  │
│                          │                                  │
│         ┌────────────────┼────────────────┐                │
│         │                │                │                │
│         ▼                ▼                ▼                │
│  ┌──────────┐    ┌──────────┐    ┌──────────┐            │
│  │ Entity 1 │    │ Entity 2 │    │ Entity N │            │
│  │  State   │    │  State   │    │  State   │            │
│  └──────────┘    └──────────┘    └──────────┘            │
│         │                │                │                │
│         └────────────────┼────────────────┘                │
│                          │                                 │
│                          ▼                                 │
│                  ┌───────────────┐                        │
│                  │  GPU Renderer │                        │
│                  └───────────────┘                        │
└─────────────────────────────────────────────────────────────┘
```

**เลเยอร์สถาปัตยกรรม:**

1. **Application Layer**: จุดเริ่มต้นที่สร้าง App object
2. **App Object Layer**: ตัวประสานกลางที่จัดการ entities และ windows ทั้งหมด
3. **Entity Layer**: components แต่ละตัวที่มี state และ behavior ของตัวเอง
4. **Rendering Layer**: Render UI elements โดยตรงไปที่ GPU

---

## ออบเจกต์ App

ออบเจกต์ App เป็นรากของแอปพลิเคชัน GPUI มันครอบครอง entities ทั้งหมดและจัดการ lifecycles ของพวกมัน

```
App Object Responsibilities:
┌─────────────────────────────────────────────┐
│  1. Entity Management                        │
│     • สร้าง entities                         │
│     • ทำลาย entities                         │
│     • ติดตาม entity ownership                │
└─────────────────────────────────────────────┘
│  2. Window Management                        │
│     • เปิด windows                           │
│     • ปิด windows                            │
│     • จัดการ window events                  │
└─────────────────────────────────────────────┘
│  3. Rendering Coordination                   │
│     • กำหนดเวลา renders                     │
│     • จัดการ render frames                  │
│     • ประสาน GPU commands                   │
└─────────────────────────────────────────────┘
│  4. Task Management                          │
│     • spawn foreground tasks                 │
│     • spawn background tasks                  │
│     • จัดการ async operations                │
└─────────────────────────────────────────────┘
│  5. Event Loop                               │
│     • ประมวลผล input events                 │
│     │ ส่ง events ไปยัง entities               │
│     • จัดการ state changes                   │
└─────────────────────────────────────────────┘
```

**ตัวอย่าง App Object:**

```rust
fn main() {
    Application::new().run(|cx: &mut App| {
        // App ครอบครอง global state entity
        let global_state = cx.new(|_| GlobalState::new());
        
        // App สร้าง window
        cx.open_window(|_, cx| {
            // Window มี workspace entity
            let workspace = cx.new(|cx| Workspace::new(global_state, cx));
            workspace
        });
    });
}
```

---

## ระบบ Entity

Entities เป็นองค์ประกอบหลักใน GPUI แต่ละ entity เป็น smart pointer ไปยัง state ที่ครอบครองโดย App object

### Entity Ownership Model

```
┌─────────────────────────────────────────────────────────┐
│                     App Object                           │
│  ┌───────────────────────────────────────────────────┐  │
│  │  Entity Store                                      │  │
│  │  ┌────────┐  ┌────────┐  ┌────────┐              │  │
│  │  │  ID: 1 │  │  ID: 2 │  │  ID: 3 │  ...        │  │
│  │  │ State  │  │ State  │  │ State  │              │  │
│  │  └────────┘  └────────┘  └────────┘              │  │
│  └───────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────┘
         │              │              │
         │ Entity<T>    │ Entity<T>    │ Entity<T>
         │ (smart ptr)  │ (smart ptr)  │ (smart ptr)
         ▼              ▼              ▼
    ┌──────────┐  ┌──────────┐  ┌──────────┐
    │ Entity 1 │  │ Entity 2 │  │ Entity 3 │
    │  Owner   │  │  Owner   │  │  Owner   │
    └──────────┘  └──────────┘  └──────────┘
```

### Entity Lifecycle

```
Entity Lifecycle:
┌────────────────────────────────────────────────────────────┐
│                                                            │
│  1. Creation                                               │
│     App::new() ──> Entity<T> ถูกจัดสรร                    │
│                                                            │
│     ┌──────────────────────────────────────────────────┐   │
│     │  cx.new(|_| MyEntity::new(cx))                   │   │
│     │      │                                            │   │
│     │      └──> จัดสรร state ใน App                   │   │
│     │      └──> คืนค่า Entity<T> (smart pointer)      │   │
│     └──────────────────────────────────────────────────┘   │
│                                                            │
│  2. Observation                                            │
│     Entity สังเกต entities อื่นผ่าน Context              │
│                                                            │
│     ┌──────────────────────────────────────────────────┐   │
│     │  cx.observe(&other_entity, |this, other, cx| {   │   │
│     │      // เรียกเมื่อ other_entity เปลี่ยนแปลง        │   │
│     │  }).detach();                                     │   │
│     └──────────────────────────────────────────────────┘   │
│                                                            │
│  3. Rendering                                              │
│     Entity render ผ่าน Render trait                      │
│                                                            │
│     ┌──────────────────────────────────────────────────┐   │
│     │  impl Render for MyEntity {                       │   │
│     │      fn render(&mut self, window, cx) {          │   │
│     │          // คืนค่า UI elements                  │   │
│     │      }                                            │   │
│     │  }                                                │   │
│     └──────────────────────────────────────────────────┘   │
│                                                            │
│  4. Destruction                                            │
│     Entity ถูกทำลายเมื่อไม่มีการอ้างอิงอีกต่อไป        │
│                                                            │
│     ┌──────────────────────────────────────────────────┐   │
│     │  // Entity<T> ออกจาก scope                      │   │
│     │  // App ล้างข้อมูล state                       │   │
│     └──────────────────────────────────────────────────┘   │
│                                                            │
└────────────────────────────────────────────────────────────┘
```

---

## ประเภท Context

GPUI ใช้หลายประเภทของ context เพื่อให้ความสามารถและ scope ที่แตกต่างกัน

### Context Hierarchy

```
┌────────────────────────────────────────────────────────────┐
│                      Context Types                         │
├────────────────────────────────────────────────────────────┤
│                                                            │
│  ┌─────────────────┐                                      │
│  │      App        │                                      │
│  │  (Root Context) │                                      │
│  │  • สร้าง windows                                     │
│  │  • สร้าง entities                                    │
│  │  • spawn tasks                                       │
│  │  • Global operations                                   │
│  └─────────────────┘                                      │
│           │                                               │
│           │ ใช้ในการสร้าง windows                        │
│           ▼                                               │
│  ┌─────────────────┐                                      │
│  │    Window       │                                      │
│  │ (Window Context)│                                      │
│  │  • Window-specific operations                        │
│  │  • Input handling                                     │
│  │  • Focus management                                    │
│  │  • Window-level rendering                             │
│  └─────────────────┘                                      │
│           │                                               │
│           │ ประกอบด้วย entities                          │
│           ▼                                               │
│  ┌─────────────────┐      ┌─────────────────┐             │
│  │  Context<T>     │      │   AsyncApp      │             │
│  │ (Entity Context)│      │ (Async Context) │             │
│  │  • Entity ops   │      │  • Async tasks  │             │
│  │  • Observe      │      │  • I/O ops      │             │
│  │  • Notify       │      │  • Non-blocking │             │
│  │  • Subscribe    │      │                 │             │
│  └─────────────────┘      └─────────────────┘             │
│                                                            │
└────────────────────────────────────────────────────────────┘
```

### Context Capabilities

```rust
// App Context - ระดับ Root
impl App {
    fn open_window(&mut self, ...) -> Entity<Window>  // สร้าง windows
    fn new<T>(&mut self, ...) -> Entity<T>            // สร้าง entities
    fn spawn(&mut self, ...) -> Task<()>              // spawn foreground tasks
}

// Context<T> - ระดับ Entity
impl<T> Context<T> {
    fn observe<U, F>(&mut self, entity: &Entity<U>, callback: F) // สังเกตการเปลี่ยนแปลง
    fn notify(&mut self)                                        // ทำให้ re-render
    fn subscribe<U, F>(&mut self, event_type: U, callback: F)   // subscribe ไปยัง events
    fn new<U>(&mut self, ...) -> Entity<U>                       // สร้าง child entities
    fn update<U, F>(&mut self, entity: &Entity<U>, update: F)    // อัปเดต state ของ entity
}

// AsyncApp - การดำเนินการ Async
impl AsyncApp {
    fn spawn<T, F>(&mut self, future: F) -> Task<T>    // spawn async tasks
    fn update<T, F>(&mut self, entity: &Entity<T>, ...) // async updates
}

// Window - การดำเนินการ Window
impl Window {
    fn use_state<T, F>(&mut self, cx: &mut Context<Self>, init: F) // UI state
    fn focus_handle(&self) -> &FocusHandle             // Focus management
}
```

---

## การจัดการ State

GPUI ใช้รูปแบบหลักสามแบบสำหรับการจัดการ state: Observe/Notify, Subscribe/Emit, และ use_state

### Pattern 1: Observe/Notify

เหมาะสำหรับการเปลี่ยนแปลง state ทั่วไปที่ entity หนึ่งต้องการตอบสนองต่อการเปลี่ยนแปลงของอีก entity หนึ่ง

```
Observer Pattern Flow:

Entity A (State Owner)              Entity B (Observer)
┌────────────────────┐               ┌────────────────────┐
│  count: usize = 0  │               │  display: String   │
└────────────────────┘               └────────────────────┘
         │                                    ▲
         │                                    │
         │ Observe                            │ Callback
         └────────────────────────────────────┘
                  │
                  │ notify()
                  ▼
         ┌────────────────────┐
         │  count: usize = 1  │  ← State เปลี่ยนแปลง
         └────────────────────┘
                  │
                  │ Entity B re-renders
                  ▼
         ┌────────────────────┐
         │  display: "Count: 1"│  ← UI อัปเดต
         └────────────────────┘
```

**ตัวอย่างโค้ด:**

```rust
struct Counter {
    count: usize,
}

impl Counter {
    pub fn increment(&mut self) {
        self.count += 1;
    }
}

struct CounterDisplay {
    counter: Entity<Counter>,
}

impl CounterDisplay {
    pub fn new(counter: &Entity<Counter>, cx: &mut Context<Self>) -> Self {
        // สังเกตการเปลี่ยนแปลงของ counter
        cx.observe(counter, |this, counter, cx| {
            // เฉพาะ entity นี้เท่านั้นที่ re-render เมื่อ counter เปลี่ยน
            cx.notify();
        }).detach();
        
        Self { counter: counter.clone() }
    }
}

impl Render for CounterDisplay {
    fn render(&mut self, window: &mut Window, cx: &mut Context<Self>) -> impl IntoElement {
        let count = self.counter.read(cx).count;
        div().child(format!("Count: {}", count))
    }
}
```

### Pattern 2: Subscribe/Emit

เหมาะสำหรับ typed events และรูปแบบ pub/sub

```
Event Subscription Flow:

Event Source (Emitter)           Event Subscriber
┌────────────────────┐           ┌────────────────────┐
│  EventEmitter<T>  │           │  Event Handler     │
└────────────────────┘           └────────────────────┘
         │                                    ▲
         │                                    │
         │ Subscribe                         │ Event Handler
         └────────────────────────────────────┘
                  │
                  │ emit(Event)
                  ▼
         ┌────────────────────┐
         │  Event: Clicked   │  ← Event ถูก emit
         └────────────────────┘
                  │
                  │ Subscribers ได้รับการแจ้งเตือน
                  ▼
         ┌────────────────────┐
         │  Handle Clicked    │  ← Event ถูกจัดการ
         └────────────────────┘
```

**ตัวอย่างโค้ด:**

```rust
#[derive(Clone, Debug)]
struct CounterChangeEvent {
    increment: usize,
}

struct Counter {
    // Implements EventEmitter
}

impl EventEmitter<CounterChangeEvent> for Counter {}

impl Counter {
    pub fn increment(&mut self, cx: &mut Context<Self>) {
        cx.emit(CounterChangeEvent { increment: 1 });
    }
}

struct CounterLogger {
    counter: Entity<Counter>,
}

impl CounterLogger {
    pub fn new(counter: &Entity<Counter>, cx: &mut Context<Self>) -> Self {
        // Subscribe ไปยัง counter events
        cx.subscribe(&counter, |this, event, cx| {
            match event {
                CounterChangeEvent { increment } => {
                    println!("Counter incremented by {}", increment);
                }
            }
        }).detach();
        
        Self { counter: counter.clone() }
    }
}
```

### Pattern 3: use_state

เหมาะสำหรับ UI state ที่ผูกกับตำแหน่ง render (hover, selection, ฯลฯ)

```
use_state Flow:

┌────────────────────────────────────────────────────────────┐
│  Component Renders                                          │
│  ┌──────────────────────────────────────────────────────┐  │
│  │  let hovered = window.use_state(cx, |_, cx| false);  │  │
│  │                                                       │  │
│  │  div()                                                │  │
│  │      .on_mouse_enter(cx.listener(|_, _, window, cx| {│  │
│  │          hovered.update(cx, |hovered, cx| {         │  │
│  │              *hovered = true;                        │  │
│  │          });                                         │  │
│  │      }))                                              │  │
│  │      .on_mouse_leave(cx.listener(|_, _, window, cx| {│  │
│  │          hovered.update(cx, |hovered, cx| {         │  │
│  │              *hovered = false;                       │  │
│  │          });                                         │  │
│  │      }))                                              │  │
│  └──────────────────────────────────────────────────────┘  │
│                           │                                 │
│                           ▼                                 │
│  ┌──────────────────────────────────────────────────────┐  │
│  │  State: false (เริ่มต้น)                              │  │
│  └──────────────────────────────────────────────────────┘  │
│                           │                                 │
│              Mouse เข้า                                   │
│                           ▼                                 │
│  ┌──────────────────────────────────────────────────────┐  │
│  │  State: true (อัปเดตแล้ว)                             │  │
│  │  Component re-renders พร้อม hovered state             │  │
│  └──────────────────────────────────────────────────────┘  │
└────────────────────────────────────────────────────────────┘
```

**ตัวอย่างโค้ด:**

```rust
struct HoverButton;

impl Render for HoverButton {
    fn render(&mut self, window: &mut Window, cx: &mut Context<Self>) -> impl IntoElement {
        let hovered = window.use_state(cx, |_, cx| false);
        
        div()
            .bg(if *hovered.read(cx) {
                rgb(0x0000ff)
            } else {
                rgb(0x000000)
            })
            .on_mouse_enter(MouseButton::Left, {
                let hovered = hovered.clone();
                cx.listener(move |_this, _event, window, cx| {
                    hovered.update(cx, |hovered, cx| {
                        *hovered = true;
                    });
                })
            })
            .on_mouse_leave(MouseButton::Left, {
                let hovered = hovered.clone();
                cx.listener(move |_this, _event, window, cx| {
                    hovered.update(cx, |hovered, cx| {
                        *hovered = false;
                    });
                })
            })
            .child("Hover me!")
    }
}
```

---

## ขั้นตอนการ Render

ขั้นตอนการ render ของ GPUI เป็นแบบตรงและมีประสิทธิภาพสูง โดยไม่มี virtual DOM layer

### Rendering Pipeline

```
┌─────────────────────────────────────────────────────────────┐
│                    GPUI Rendering Pipeline                  │
└─────────────────────────────────────────────────────────────┘

1. State Change เกิดขึ้น
   │
   ▼
   ┌──────────────────────────────────────────────────┐
   │  Entity A เรียก notify() หรือ state เปลี่ยน       │
   │  ผ่าน use_state, observe, หรือ subscribe          │
   └──────────────────────────────────────────────────┘
   │
   ▼
2. App กำหนดเวลา Render
   │
   ▼
   ┌──────────────────────────────────────────────────┐
   │  App ระบุ entities ที่ต้อง re-render:            │
   │  • Entity A (ทำให้เกิดการเปลี่ยนแปลง)             │
   │  • Observers ของ Entity A (ผ่าน observe)         │
   │  • UI elements ที่ใช้ state ที่เปลี่ยน (use_state) │
   └──────────────────────────────────────────────────┘
   │
   ▼
3. Entities Render
   │
   ▼
   ┌──────────────────────────────────────────────────┐
   │  สำหรับ entity ที่ได้รับผลกระทบแต่ละตัว:        │
   │  ┌────────────────────────────────────────────┐   │
   │  │  impl Render for MyEntity {               │   │
   │  │      fn render(&mut self, window, cx) {  │   │
   │  │          // คืนค่า UI elements            │   │
   │  │          div()                             │   │
   │  │              .child("Hello")               │   │
   │  │      }                                    │   │
   │  │  }                                        │   │
   │  └────────────────────────────────────────────┘   │
   └──────────────────────────────────────────────────┘
   │
   ▼
4. Elements ถูกประกอบ
   │
   ▼
   ┌──────────────────────────────────────────────────┐
   │  UI elements ถูกประกอบเป็น render tree:          │
   │                                                  │
   │  ┌─────────────────────────────────────────┐     │
   │  │  Window                                 │     │
   │  │    ┌─────────────────────────────┐      │     │
   │  │    │  Parent Entity               │      │     │
   │  │    │    ┌──────────────────┐     │      │     │
   │  │    │    │  Child Entity    │     │      │     │
   │  │    │    │    Text "Hello"  │     │      │     │
   │  │    │    └──────────────────┘     │      │     │
   │  │    └─────────────────────────────┘      │     │
   │  └─────────────────────────────────────────┘     │
   └──────────────────────────────────────────────────┘
   │
   ▼
5. GPU Rendering
   │
   ▼
   ┌──────────────────────────────────────────────────┐
   │  GPU commands ถูกสร้างโดยตรง:                   │
   │  • Vertex data                                    │
   │  • Fragment shaders                               │
   │  • Texture binding                                │
   │  • Draw calls                                     │
   └──────────────────────────────────────────────────┘
   │
   ▼
6. Display
   │
   ▼
   ┌──────────────────────────────────────────────────┐
   │  GPU render ไปยังหน้าจอ                           │
   │  • ไม่มี virtual DOM diffing                       │
   │  • ไม่มี intermediate layers                      │
   │  • Direct pixel output                            │
   └──────────────────────────────────────────────────┘
```

### Rendering Characteristics

```
GPUI Rendering Characteristics:

┌────────────────────────────────────────────────────────────┐
│  ไม่มี Virtual DOM                                          │
│  • Render โดยตรงไปยัง GPU                                 │
│  • ไม่มี diffing algorithm                                │
│  • ไม่มี reconciliation step                               │
└────────────────────────────────────────────────────────────┘
│                                                            │
│  Selective Re-rendering                                     │
│  • เฉพาะ entities ที่ได้รับผลกระทบเท่านั้นที่ re-render  │
│  • Observers อัปเดตเฉพาะเมื่อ state ที่สังเกตเปลี่ยน    │
│  • ไม่มี cascade re-renders                              │
└────────────────────────────────────────────────────────────┘
│                                                            │
│  GPU Accelerated                                            │
│  • การ render ทั้งหมดเกิดขึ้นบน GPU                     │
│  • Hardware-accelerated transforms                         │
│  • Animations และ transitions ที่ราบรื่น                  │
└────────────────────────────────────────────────────────────┘
│                                                            │
│  Hybrid Mode                                                │
│  • Retained mode สำหรับ state ถาวร                        │
│  • Immediate mode สำหรับ components ชั่วคราว              │
│  • ได้สิ่งที่ดีที่สุดจากทั้งสองแบบ                      │
└────────────────────────────────────────────────────────────┘
```

---

## ระบบเหตุการณ์

ระบบเหตุการณ์ของ GPUI มีประสิทธิภาพและเป็นเป้าหมาย โดย events จะไปถึง entities เฉพาะที่จัดการอย่างชัดเจนเท่านั้น

### Event Flow

```
Event Propagation Flow:

User Input (Mouse/Keyboard)
         │
         ▼
┌──────────────────────────────────────────────────┐
│  Window Event Queue                               │
│  ┌────────────────────────────────────────────┐   │
│  │  MouseEvent(MouseDown, x: 100, y: 200)    │   │
│  │  KeyEvent(KeyPress, key: 'A')             │   │
│  │  MouseEvent(MouseUp, x: 100, y: 200)      │   │
│  └────────────────────────────────────────────┘   │
└──────────────────────────────────────────────────┘
         │
         ▼
┌──────────────────────────────────────────────────┐
│  Event Dispatch ไปยัง Entities                     │
│  • Target entity ที่มี event handler              │
│  • เฉพาะ entities ที่มี handlers เท่านั้นที่รับ events │
│  • ไม่มี event bubbling โดยค่าเริ่มต้น           │
└──────────────────────────────────────────────────┘
         │
         ▼
┌──────────────────────────────────────────────────┐
│  Event Handler Execution                          │
│  ┌────────────────────────────────────────────┐   │
│  │  div()                                      │   │
│  │      .on_mouse_down(cx.listener(            │   │
│  │          |this, event, window, cx| {       │   │
│  │              // จัดการ mouse down           │   │
│  │          }                                  │   │
│  │      ))                                     │   │
│  └────────────────────────────────────────────┘   │
└──────────────────────────────────────────────────┘
         │
         ▼
┌──────────────────────────────────────────────────┐
│  State Changes & Re-renders                        │
│  • Handler อาจทำให้เกิดการเปลี่ยนแปลง state     │
│  • Entities ที่ได้รับผลกระทบ re-render            │
│  • GPU render UI ที่อัปเดต                        │
└──────────────────────────────────────────────────┘
```

### Event Types

```rust
// Mouse Events
MouseButton::Left       // ปุ่มเมาส์ซ้าย
MouseButton::Right      // ปุ่มเมาส์ขวา
MouseButton::Middle     // ปุ่มเมาส์กลาง
MouseButton::Back       // ปุ่มย้อนกลับ
MouseButton::Forward    // ปุ่มไปข้างหน้า

// Keyboard Events
Key::Character('a')    // ปุ่มตัวอักษร
Key::Up                // ปุ่มลูกศรขึ้น
Key::Down              // ปุ่มลูกศรลง
Key::Enter             // ปุ่ม Enter
Key::Escape            // ปุ่ม Escape

// Event Handler Registration
div()
    .on_mouse_down(MouseButton::Left, cx.listener(|...| { }))
    .on_mouse_up(MouseButton::Left, cx.listener(|...| { }))
    .on_mouse_move(cx.listener(|...| { }))
    .on_key_down(cx.listener(|...| { }))
    .on_key_up(cx.listener(|...| { }))
```

---

## โมเดลประสิทธิภาพ

สถาปัตยกรรมของ GPUI ถูกออกแบบมาเพื่อประสิทธิภาพสูงกับ UI ที่ซับซ้อน

### Performance Characteristics

```
┌────────────────────────────────────────────────────────────┐
│                    GPUI Performance Model                   │
├────────────────────────────────────────────────────────────┤
│                                                            │
│  1. ไม่มี Cascade Re-renders                                │
│     ┌─────────────────────────────────────────────────┐   │
│     │  Parent State Change                            │   │
│     │  • เฉพาะ observing children เท่านั้นที่ re-render│
│     │  • ไม่มี automatic child re-renders              │
│     │  • Explicit observation เท่านั้น               │
│     └─────────────────────────────────────────────────┘   │
│                                                            │
│  2. Zero Virtual DOM Overhead                              │
│     ┌─────────────────────────────────────────────────┐   │
│     │  • Direct GPU rendering                         │   │
│     │  • ไม่มี diffing algorithm                      │   │
│     │  • ไม่มี reconciliation                        │   │
│     │  • ไม่มีการจัดสรร memory ของ virtual DOM      │   │
│     └─────────────────────────────────────────────────┘   │
│                                                            │
│  3. Selective Observation                                 │
│     ┌─────────────────────────────────────────────────┐   │
│     │  • Entities สังเกตเฉพาะสิ่งที่ต้องการ         │
│     │  • การแจ้งเตือนการเปลี่ยนแปลงแบบละเอียด    │
│     │  • ขอบเขต re-render ที่น้อยที่สุด            │
│     │  • ไม่มีการอัปเดตที่ไม่จำเป็น                 │
│     └─────────────────────────────────────────────────┘   │
│                                                            │
│  4. GPU Acceleration                                      │
│     ┌─────────────────────────────────────────────────┐   │
│     │  • Hardware-accelerated rendering               │   │
│     │  • Efficient transforms                         │   │
│     │  • Animations ที่ราบรื่น                       │   │
│     │  • Parallel processing                          │   │
│     └─────────────────────────────────────────────────┘   │
│                                                            │
│  5. Efficient Memory Usage                                 │
│     ┌─────────────────────────────────────────────────┐   │
│     │  • Smart pointer references (Arc)               │   │
│     │  • ไม่มีการคัดลอก state ซ้ำ                 │   │
│     │  • การเข้าถึง state ร่วมกัน                 │   │
│     │  • การจัดสรรขั้นต่ำ                           │   │
│     └─────────────────────────────────────────────────┘   │
│                                                            │
└────────────────────────────────────────────────────────────┘
```

### Performance Comparison

```
React vs GPUI Performance:

Deep Component Tree (100 levels)
┌────────────────────────────────────────────────────────────┐
│  React                                                      │
│  ┌──────────────────────────────────────────────────────┐  │
│  │  Parent State Change                                 │  │
│  │  → 100 components re-render                         │  │
│  │  → Virtual DOM diffing สำหรับ 100 components          │  │
│  │  → Reconciliation สำหรับ 100 components              │  │
│  │  → Actual DOM updates สำหรับ 100 components           │  │
│  │  เวลา: ~50-100ms (โดยไม่มีการปรับปรุง)         │  │
│  └──────────────────────────────────────────────────────┘  │
│                                                            │
│  GPUI                                                       │
│  ┌──────────────────────────────────────────────────────┐  │
│  │  Parent State Change                                 │  │
│  │  → เฉพาะ 1 component เท่านั้นที่ re-render (parent) │  │
│  │  → Direct GPU render ของ component ที่เปลี่ยน       │  │
│  │  → ไม่มี virtual DOM                                 │  │
│  │  → ไม่มี reconciliation                              │  │
│  │  เวลา: ~1-5ms                                        │  │
│  └──────────────────────────────────────────────────────┘  │
│                                                            │
│  การปรับปรุงประสิทธิภาพ: เร็วกว่า 10-100 เท่า         │
└────────────────────────────────────────────────────────────┘
```

---

## การเปรียบเทียบกับ React

### Architecture Comparison

```
┌─────────────────────────────────────────────────────────────┐
│                    React Architecture                       │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  ┌─────────────────────────────────────────────────────┐   │
│  │  React Component Tree                               │   │
│  │                                                      │   │
│  │  App                                                │   │
│  │    └─ Parent (State)                               │   │
│  │         ├─ Child A (Props) ← Re-renders!            │   │
│  │         ├─ Child B (Props) ← Re-renders!            │   │
│  │         └─ Child C (Props) ← Re-renders!            │   │
│  │                                                      │   │
│  └─────────────────────────────────────────────────────┘   │
│                          │                                  │
│                          ▼                                  │
│  ┌─────────────────────────────────────────────────────┐   │
│  │  Virtual DOM                                        │   │
│  │  • Diffing algorithm                                │   │
│  │  • Reconciliation                                   │   │
│  │  • Batch updates                                    │   │
│  └─────────────────────────────────────────────────────┘   │
│                          │                                  │
│                          ▼                                  │
│  ┌─────────────────────────────────────────────────────┐   │
│  │  Actual DOM                                         │   │
│  │  • Browser DOM API                                  │   │
│  │  • Reflows และ repaints                            │   │
│  └─────────────────────────────────────────────────────┘   │
│                                                             │
│  ปัญหา:                                                    │
│  • ทุก children re-render เมื่อ parent เปลี่ยน            │
│  • ต้องการ React.memo, useMemo, useCallback เพื่อ optimize│
│  • Prop drilling สำหรับ shared state                      │
│  • Virtual DOM overhead                                   │
│                                                             │
└─────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────┐
│                     GPUI Architecture                       │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  ┌─────────────────────────────────────────────────────┐   │
│  │  App Object (ตัวประสานกลาง)                         │   │
│  │  • ครอบครอง entities ทั้งหมด                          │
│  │  • จัดการ lifecycles                                │   │
│  └─────────────────────────────────────────────────────┘   │
│                          │                                  │
│          ┌───────────────┼───────────────┐                  │
│          ▼               ▼               ▼                  │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐        │
│  │ Entity A    │  │ Entity B    │  │ Entity C    │        │
│  │ (Parent)    │  │ (Child A)   │  │ (Child B)   │        │
│  │             │  │ Observes A  │  │ Independent │        │
│  └─────────────┘  └─────────────┘  └─────────────┘        │
│                                                              │
│                          │                                   │
│                          ▼                                   │
│  ┌─────────────────────────────────────────────────────┐   │
│  │  GPU Renderer                                      │   │
│  │  • Direct rendering (ไม่มี virtual DOM)            │   │
│  │  • Hardware-accelerated                            │   │
│  │  • Immediate mode สำหรับ transient components    │   │
│  └─────────────────────────────────────────────────────┘   │
│                                                             │
│  ข้อดี:                                                    │
│  • เฉพาะ observing entities เท่านั้นที่ re-render      │
│  • ไม่ต้องการ memoization                                 │
│  • Direct entity observation (ไม่มี prop drilling)       │
│  • Zero virtual DOM overhead                              │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

### State Management Comparison

```
React State Management:
┌────────────────────────────────────────────────────────────┐
│  State อยู่ใน components                                    │
│  • useState hook                                            │
│  • Props ไหลลง                                            │
│  • Context API สำหรับ shared state                        │
│  • External libraries สำหรับ state ที่ซับซ้อน              │
│                                                             │
│  Parent → Child A (Props) → Child A1 (Props)               │
│               ↓                                           │
│            Child B (Props)                                 │
│                                                             │
│  ปัญหา: Prop drilling สร้าง coupling ที่แน่นแฟ้น      │
└────────────────────────────────────────────────────────────┘

GPUI State Management:
┌────────────────────────────────────────────────────────────┐
│  State อยู่ใน entities ที่ครอบครองโดย App                │
│  • Entities ครอบครอง state                                 │
│  • Entities สังเกตกันโดยตรง                             │
│  • ไม่มี prop drilling                                    │
│  • สามรูปแบบ: observe/notify, subscribe/emit, use_state   │
│                                                             │
│  ┌─────────────┐                                           │
│  │  App Object  │                                           │
│  └─────────────┘                                           │
│         │                                                   │
│         ├── Entity A (ครอบครอง State A)                    │
│         │       ▲                                          │
│         │       │ สังเกต                                  │
│         │       │                                         │
│         │   Entity B (อ่าน State A)                       │
│         │       ▲                                          │
│         │       │ สังเกต                                  │
│         │       │                                         │
│         │   Entity C (อ่าน State A)                       │
│         │                                                   │
│         └── Entity D (ครอบครอง State D, independent)       │
│                                                             │
│  ข้อดี: Entities สังเกตโดยตรง, ไม่มี coupling         │
└────────────────────────────────────────────────────────────┘
```

### Re-rendering Behavior

```
React Re-rendering:
┌────────────────────────────────────────────────────────────┐
│  Trigger: Parent state change                              │
│                                                             │
│  Parent (changed)                                          │
│    ├─ Child A → RE-RENDERS ❌                             │
│    ├─ Child B → RE-RENDERS ❌                             │
│    ├─ Child C → RE-RENDERS ❌                             │
│    └─ Child D → RE-RENDERS ❌                             │
│                                                             │
│  ต้องการ Optimization:                                     │
│  • React.memo สำหรับ child แต่ละตัว                     │
│  • useMemo สำหรับ computations ที่แพง                  │
│  • useCallback สำหรับ function references                 │
│  • ต้องการ manual optimization                           │
│                                                             │
└────────────────────────────────────────────────────────────┘

GPUI Re-rendering:
┌────────────────────────────────────────────────────────────┐
│  Trigger: Parent state change                               │
│                                                             │
│  Parent (changed) → RE-RENDERS ✅                         │
│    ├─ Child A (สังเกต parent) → RE-RENDERS ✅            │
│    ├─ Child B (independent) → NO RE-RENDER ✅              │
│    ├─ Child C (independent) → NO RE-RENDER ✅              │
│    └─ Child D (independent) → NO RE-RENDER ✅              │
│                                                             │
│  Built-in Optimization:                                      │
│  • เฉพาะ observing entities เท่านั้นที่ re-render       │
│  • ไม่ต้องการ memoization                                │
│  • ไม่มีการอัปเดตที่ไม่จำเป็น                         │
│  • Efficient โดยการออกแบบ                               │
│                                                             │
└────────────────────────────────────────────────────────────┘
```

### Key Differences Summary

```
┌────────────────────────────────────────────────────────────┐
│                GPUI vs React: ความแตกต่างหลัก          │
├──────────────┬───────────────────────┬──────────────────────┤
│  Feature     │  React                │  GPUI                │
├──────────────┼───────────────────────┼──────────────────────┤
│  Architecture│  Component-based     │  Entity-based        │
│  State       │  In components       │  In entities         │
│  Rendering   │  Virtual DOM         │  Direct GPU          │
│  Re-renders  │  All children        │  Observers only      │
│  Prop Drilling│  Required           │  Not needed          │
│  Optimization│  Manual (memo)      │  Built-in            │
│  Performance │  Good (needs opt)   │  Excellent           │
│  Language    │  JavaScript          │  Rust                │
│  Platform    │  Web, Mobile, Desktop│  Desktop            │
├──────────────┼───────────────────────┼──────────────────────┤
│  Best For    │  Web apps            │  Desktop apps        │
│              │  Rapid prototyping   │  Complex UIs         │
│              │  Teams with JS exp   │  High performance    │
└──────────────┴───────────────────────┴──────────────────────┘
```

---

## บทสรุป

GPUI เป็นแนวทางที่แตกต่างอย่างสิ้นเชิงสำหรับการพัฒนา UI เมื่อเทียบกับ React ในขณะที่ React เน้นที่ประสบการณ์ของนักพัฒนาและการเขียนโปรแกรมแบบประกาศ GPUI ให้ความสำคัญกับประสิทธิภาพและความมีประสิทธิภาพผ่านสถาปัตยกรรมแบบ entity-based

**สิ่งสำคัญที่ควรจำ:**

1. **Entity-Based Architecture**: State อยู่ใน entities ที่ครอบครองโดย App ไม่ใช่ใน components
2. **Selective Observation**: เฉพาะ entities ที่สังเกต state ที่เปลี่ยนเท่านั้นที่จะ re-render
3. **Direct GPU Rendering**: ไม่มี virtual DOM layer, render โดยตรงสำหรับประสิทธิภาพสูงสุด
4. **Built-in Optimization**: ไม่ต้องการเทคนิค memoization แบบ manual
5. **Explicit Patterns**: การแยกความรับผิดชอบที่ชัดเจนด้วย observe/notify, subscribe/emit, และ use_state

GPUI เหมาะเป็นพิเศษสำหรับแอปพลิเคชัน desktop ที่มี UI ที่ซับซ้อน ต้นไม้ components ที่ลึก และความต้องการประสิทธิภาพสูง แม้ว่าจะมี learning curve ที่ชันกว่าเนื่องจาก Rust และแนวคิดใหม่ แต่มันจ่ายค่าด้วยประสิทธิภาพที่ดีขึ้นอย่างมีนัยสำคัญและการจัดการ state ที่ง่ายขึ้น

---

## แหล่งข้อมูลเพิ่มเติม

- [GPUI Overview](./overview.md) - คู่มือการใช้งาน GPUI อย่างครบถ้วน
- [GPUI vs React Comparison](./compare_react.md) - การเปรียบเทียบโดยละเอียดกับ React
- [GPUI Examples](../examples/) - แอปพลิเคชันตัวอย่างที่แสดงแนวคิดของ GPUI